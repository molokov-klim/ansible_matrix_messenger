---
# vars/main.yml
matrix_db_name: "synapse"
matrix_db_user: "synapse_user"
matrix_db_password: "{{ lookup('env', 'POSTGRES_PASSWORD') | default('default_password_for_testing') }}"

# Пользовательские переменные
admin_username: "{{ lookup('env', 'MATRIX_ADMIN_USERNAME') | default('admin') }}"
family_users: "{{ lookup('env', 'FAMILY_USERS') | default([]) }}"